llvm-project directory is the entire LLVM monorepo, don't try analyzing it, it won't fit into your context. Just assume that it's LLVM 20

Use docker's buildx builder, image won't build properly with legacy one
